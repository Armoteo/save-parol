import React from 'react'
import { StyleSheet, View, Text } from 'react-native'
import AppText from '../components/UI/AppText'

export const EditScreen = props => {
  return (
    <View>
      <AppText>EditScreen</AppText>
    </View>
  )
}

const styles = StyleSheet.create({

})